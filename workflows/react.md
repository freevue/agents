# Get React Rule

React와 관련된 Rule만 가지고와 리스트업합니다.

## Rules

아래의 목록에 있는 파일들을 `view_file`을 이용하여 읽은 후 Rule을 적용하세요.

- Common Code Convention: `경로`
- React Code Convention: `경로`
- React Architecture: `경로`
